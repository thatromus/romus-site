---
import AboutC from "@layouts/About.astro";
import { getEntry, render } from "astro:content";

const layout = await getEntry("about", "cc");

if (!layout) {
  throw new Error("Запись about не найдена!");
}
const { Content } = await render(layout);
---

<AboutC {...layout.data}>
  <Content />
</AboutC>
